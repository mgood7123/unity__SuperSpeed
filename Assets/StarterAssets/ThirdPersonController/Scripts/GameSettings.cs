using UnityEngine;

public class GameSettings : MonoBehaviour {

    // affects render only
    public static int render_fps = 60;

    // affects render + timers
    public static int timer_fps = 60;

}